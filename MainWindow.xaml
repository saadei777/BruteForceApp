<Window x:Class="BruteForceApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Brute-Force Password Cracker" Height="680" Width="640"
        Background="#1E1E2E" ResizeMode="CanMinimize">

    <Window.Resources>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#89B4FA"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B4BEFE"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#45475A"/>
                                <Setter Property="Foreground" Value="#585B70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#F38BA8"/>
        </Style>
        <Style x:Key="GreenButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#A6E3A1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Brute-Force Password Cracker"
                   FontSize="22" FontWeight="Bold" Foreground="#CDD6F4"
                   HorizontalAlignment="Center" Margin="0,0,0,16"/>

        <!-- Password Creation Panel -->
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="PASSWORD CREATION" Style="{StaticResource HeaderText}"
                           Foreground="#A6E3A1" Margin="0,0,0,10"/>
                <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Plain Password:" Style="{StaticResource HeaderText}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtPlainPassword" Grid.Column="1"
                               Style="{StaticResource ValueText}" Text="—" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="SHA256 Hash:" Style="{StaticResource HeaderText}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtHash" Grid.Column="1"
                               Style="{StaticResource ValueText}" Text="—"
                               TextWrapping="Wrap" VerticalAlignment="Center" FontSize="10"/>
                    <Button x:Name="BtnGenerate" Grid.Column="2" Content="Generate Password"
                            Style="{StaticResource PrimaryButton}"
                            Click="BtnGenerate_Click" Margin="12,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Attack Control Panel -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="ATTACK CONTROL" Style="{StaticResource HeaderText}"
                           Foreground="#FAB387" Margin="0,0,0,10"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Threads in use: " Style="{StaticResource HeaderText}"/>
                        <TextBlock x:Name="TxtThreadCount" Style="{StaticResource ValueText}" Text="—"/>
                    </StackPanel>
                    <Button x:Name="BtnStart" Grid.Column="1" Content="▶  Start Attack"
                            Style="{StaticResource PrimaryButton}"
                            Click="BtnStart_Click" IsEnabled="False"/>
                    <Button x:Name="BtnStop" Grid.Column="3" Content="■  Stop"
                            Style="{StaticResource DangerButton}"
                            Click="BtnStop_Click" IsEnabled="False"/>
                    <Button x:Name="BtnBenchmark" Grid.Column="5" Content="⚡ Benchmark"
                            Style="{StaticResource GreenButton}"
                            Click="BtnBenchmark_Click" IsEnabled="False"/>
                </Grid>

                <ProgressBar x:Name="ProgressBar" Height="14" Minimum="0" Maximum="100"
                             Background="#45475A" Foreground="#89B4FA"
                             BorderThickness="0" Margin="0,0,0,8"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Progress: " Style="{StaticResource HeaderText}"/>
                        <TextBlock x:Name="TxtProgress" Style="{StaticResource ValueText}" Text="0%"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Checked: " Style="{StaticResource HeaderText}"/>
                        <TextBlock x:Name="TxtChecked" Style="{StaticResource ValueText}" Text="0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Elapsed: " Style="{StaticResource HeaderText}"/>
                        <TextBlock x:Name="TxtElapsed" Style="{StaticResource ValueText}" Text="0.000 s"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Result Panel -->
        <Border Grid.Row="3" Background="#313244" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Found Password:" Style="{StaticResource HeaderText}" VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtFoundPassword" Grid.Column="1"
                           Text="—" FontSize="20" FontWeight="Bold"
                           Foreground="#A6E3A1" FontFamily="Consolas" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Log Output -->
        <Border Grid.Row="4" Background="#313244" CornerRadius="8" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="LOG / BENCHMARK OUTPUT"
                           Style="{StaticResource HeaderText}"
                           Foreground="#CBA6F7" Margin="0,0,0,8"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" x:Name="LogScroller">
                    <TextBox x:Name="TxtLog"
                             Background="Transparent" Foreground="#CDD6F4"
                             FontFamily="Consolas" FontSize="11"
                             BorderThickness="0" IsReadOnly="True"
                             TextWrapping="Wrap" AcceptsReturn="True"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
