# Node (only used to render the UML diagram)
node_modules/
package-lock.json

# Build output (a compiled copy is shipped separately in the Moodle zip)
bin/
obj/

# Word/Office lock & temp files
~$*

# Visual Studio cache / user files
.vs/
*.suo
*.user
